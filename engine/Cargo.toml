[package]
name = "engine"
version = "1.1.0-dev"
edition = "2021"

[[bin]]
name = "engine"
path = "src/main.rs"

[dependencies]

[features]
default = []
# Per-phase timers inside the v3 incremental update (read by `engine nnuebench`). Off in production.
nnue_profile = []
