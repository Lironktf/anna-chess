[workspace]
resolver = "2"
members = ["engine"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false

[profile.release-debug]
inherits = "release"
debug = true
